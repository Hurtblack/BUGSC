package com.euedrc.bugsc.data

/**
 * Public source marker: backend implementations are intentionally excluded from app/src/main.
 * The open-source build installs disabled data sources through DistributionServices.
 */
object NoBackendExposure
