<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/hud_screen_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="飞船查找 // BETA"
            android:textColor="@color/sc_accent"
            android:textSize="12sp" />

        <AutoCompleteTextView
            android:id="@+id/et_ship_id"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/input_bg"
            android:completionThreshold="1"
            android:hint="搜索船，例如：MOLE / Polaris / Zeus"
            android:padding="12dp"
            android:textColor="@color/sc_text"
            android:textColorHint="@color/sc_text_dim" />

        <TextView
            android:id="@+id/tv_ship_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/card_bg"
            android:padding="12dp"
            android:text="请选择船只"
            android:textColor="@color/sc_text_mid"
            android:textSize="13sp" />

        <ImageView
            android:id="@+id/iv_ship"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:layout_marginTop="8dp"
            android:adjustViewBounds="true"
            android:background="@drawable/card_bg"
            android:contentDescription="ship image"
            android:scaleType="fitCenter"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btn_official_url"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="官网 ↗"
                android:visibility="gone" />

            <Space
                android:layout_width="8dp"
                android:layout_height="1dp"
                android:visibility="gone" />

            <Button
                android:id="@+id/btn_loadout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="配装 →"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
