> 本清单只列**人工操作或观察**的核心验证项。
> 代码结构、设计对齐、scenario 代码核对、单测覆盖等已由 tasks.md review 组自动覆盖。

## 1. 构建与测试

- [x] `./gradlew :app:assembleDebug` 通过
- [x] `./gradlew :app:testDebugUnitTest` 全绿
- [ ] `./gradlew :app:lintDebug` 无新增警告
  当前 lint 剩余 14 个阻断 error，均位于本次未修改的 `ImageDiskCache.kt` 与 `ShipFitCodec.kt`；本次新增文件无 lint error。

## 2. 核心端到端验证

- [ ] 在出售挂单详情选择数量、三级地点、填写运费并提交 → 显示后台返回的交易编号，点击查看详情展示相同编号
- [ ] 对同一挂单已有进行中交易时再次提交 → 创建被阻止，并能进入现有交易
- [ ] 在非交易时间打开挂单详情 → 显示建议联系交易者的提示，联系和创建按钮仍可使用
- [ ] 从 SCM 账号进入“我的交易”，切换四种筛选并打开详情 → 列表状态匹配，详情无状态操作按钮
- [ ] 从 SCM 账号进入“消息”，打开含未读的会话 → 进入聊天页后未读数字减少或消失

## 3. 本次变更的特定风险

- [ ] 使用仍返回数据库 ID 的旧后台创建接口提交 → App 明确提示接口契约不兼容，不跳转到错误详情
- [ ] App 前台收到其他会话新消息后切到个人页 → 底部个人导航出现红点，消息入口显示服务端未读数
- [ ] 收到消息后将 App 切到后台再返回 → 后台期间无常驻通知，返回后连接恢复且未读数与服务端一致
